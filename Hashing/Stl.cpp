#include<bits/stdc++.h>
using namespace std;

//maps
// unordered maps is implemented using hash tables
//Insertion - O(1) Accessing - O(1)
//maps are implemented using Red-Black Trees
// Insertion- O(log(n)) Accessing - O(log(n))

int main()
{
    int n;
    cin>>n;
    int arr[n];
    for(int i=0; i<n; i++){
        cin>>arr[n];
    }
    map<int,int> m;
    



}